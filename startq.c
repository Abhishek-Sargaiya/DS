// program for check the no. is palindrome or not 
#include <stdio.h>
void main()
{
    int n,i
    printf("Enter the number :");
    scanf("%d", &n);
    for ( i = n; i <= n; i++)
    {
        if (    )
        {
            /* code */
        }
        
    }
    
}