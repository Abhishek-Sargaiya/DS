#include <bits/stdc++.h>
#include "class.cpp"
using namespace std;
Node* splitLL(Node* &head){
    Node* temp = head;
    
}
int main(){
    Node* head= makeLL();
    printLL(head);
    float x = countLL(head);
    cout<<x;
    splitLL(head);
}